[defaults]
inventory = inventory.ini
host_key_checking = False
retry_files_enabled = False
timeout = 30
remote_user = ubuntu
forks = 10

# reduce ssh verbosity from ansible itself (optional)
# log_path = ansible.log
